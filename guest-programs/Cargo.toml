[profile.release]
lto = "fat"
panic = "abort"
opt-level = 3
codegen-units = 1
debug = true

[workspace]
resolver = "2"
members = [
    # Examples:
    "example-hello-world",

    # Benchmarks:
    "bench-pinky",
    "bench-prime-sieve",
]
